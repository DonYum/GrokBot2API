{
  "name": "grokbot2api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private Grok CLI Responses sidecar for Grok Bot inference entitlement.",
  "bin": {
    "grokbot2api": "bin/grokbot2api.mjs"
  },
  "scripts": {
    "start": "node bin/grokbot2api.mjs",
    "check": "node --check bin/grokbot2api.mjs && node --check src/*.mjs && node --check test/*.test.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
